# RH-lane handoff to the external maintainer (2026-08-06)

Status: **active handoff.** The session budget that produced PR #298/#299/#300
is exhausted for this week. Per the owner's instruction, the external
reviewer/maintainer ("GPT") takes over *execution* of the remaining pillars,
not only review. Nothing about the trust model changes: **the Lean kernel via
CI remains the sole judge of correctness**, every model (including the
maintainer's own drafting) is a drafter only, and every promotion still goes
through a PR with the independent gates green.

## 1. State snapshot

Merged and kernel-checked:

- PR #298 (`d6e146fa`) — S0-TRUST ledger machinery (registry, per-row axiom
  audit, isolation gate). Barrier `S0-TRUST` CLOSED.
- PR #299 (`288d65b`) — built module
  `ResearchOS/AnalyticNumberTheory/RiemannHypothesis/TargetBridge.lean`
  (8 root-namespace theorems, P1–P5). Barrier `S1-TARGET` CLOSED.

In PR #300 (this branch, pending your review):

- RH-004 closure bookkeeping; RH-006 source replay record
  (`notes/reviews/RH006_SOURCE_REPLAY_2026_08_06.md`, 56/58 confirmed, two
  discrepancies flagged); RH-007 gate; frozen `CONJ_SYMMETRY_CONTRACT.md`
  (draft v2); the non-built draft `drafts/RiemannConj.lean` (Z1–Z9, three-lens
  review, `LIKELY_ELABORATES`, zero S0/S1/S2); this handoff.

Non-built drafts staged for promotion (drafts lane, kernel-unchecked):

- `drafts/RiemannXi.lean` — X1–X11 (`LIKELY_ELABORATES`).
- `drafts/RiemannConj.lean` — Z1–Z9 (`LIKELY_ELABORATES`); Z8 needs the built
  bridge import; Z7/Z9-xi need the future Xi module.

## 2. Assignments, in strict order

**A1 — finish and merge PR #300.** Markdown/drafts only, no built Lean
changes; CI already green at `cebf17f` + this commit.

**A2 — close RH-006 (source-contract acceptance).** Apply the two amendments
to `SOURCE_CONTRACTS.md` recorded in `RH006_SOURCE_REPLAY_2026_08_06.md`:

1. `SC-WEIL-01`: the source's *named* involution in LAG07 p. 1704 is the
   linear `G(1−s)`; quote that as SOURCE and re-classify the conjugated map
   `s ↦ conj(G(1−conj s))` as DERIVED (it appears only inside (A.7)'s
   composite). Until amended, no Lean statement may cite that line as SOURCE.
2. `SC-NB-04` erratum-2 quotation: letterform `dt`, not `d τ` (cosmetic).

Use a dated amendment section — never rewrite the frozen text in place. Then
set RH-006 → COMPLETE in `tasks/RIEMANN_HYPOTHESIS.md` and activate RH-007
(the queue gate enforces exactly one ACTIVE task).

**A3 — execute RH-007 (xi promotion, closes `S1-XI`).** Mirror PR #299's
recipe (see `notes/reviews/RH_BRIDGE_PROMOTION_2026_08_06.md`):

1. Copy `drafts/RiemannXi.lean` →
   `ResearchOS/AnalyticNumberTheory/RiemannHypothesis/Xi.lean`; replace the
   draft header with a built-module header; make the bridge import real
   (`import ResearchOS.AnalyticNumberTheory.RiemannHypothesis.TargetBridge`);
   change nothing else without re-review.
2. Same PR: `RH-XI-*` rows in `VERIFIED_RESEARCHOS.md` (12-cell format,
   statement anchors `file:line@sha256:<12hex>`), regenerate registry + axiom
   audit (`scripts/gen_researchos_registry.py`, `scripts/gen_axiom_audit.py`),
   promotion review record in `notes/reviews/`. The inverse-coverage gate
   fails CI if any of these is missing.
3. CI green = kernel verdict. If the kernel rejects a step, use the inline
   fallback comments in the draft (that is what they are for) — never weaken a
   statement. On merge: dated `S1-XI` CLOSED addendum in
   `MATHLIB_CAPABILITY_MAP.md`, queue bookkeeping, rollback point recorded.

**A4 — S1-CONJ promotion (after Xi lands).**

1. First add a dated erratum addendum to `CONJ_SYMMETRY_CONTRACT.md`: Annex B
   fix F1 (`linear_combination h - hΛ`) is itself wrong-signed; the correct
   closer is `hΛ - h` (three independent re-derivations; see the
   `CONTRACT ERRATUM` comment at the Z3 site of `drafts/RiemannConj.lean`).
   Do not "restore" the contract's sign at build time.
2. Promote `drafts/RiemannConj.lean` →
   `ResearchOS/AnalyticNumberTheory/RiemannHypothesis/Conj.lean` with the two
   real imports (TargetBridge, Xi), `RH-CONJ-*` ledger rows, regenerated
   registry/audit, promotion record — one PR, same gates.
3. **Honest boundary:** promotion does NOT close `S1-CONJ`. The barrier's exit
   evidence also requires divisor invariance under `ρ ↦ 1−conj ρ`, which
   belongs to the divisor package (A5). Record "conjugation leg landed" only.

**A5 — next pillars (foundation order, per `MATHLIB_CAPABILITY_MAP.md`).**
`S1-MULTIPLICITY` first: a divisor/multiplicity package contract in the same
statement-first protocol (pinned-API verification at
`fabf563a7c95a166b8d7b6efca11c8b4dc9d911f`, adversarial review, freeze, draft,
promote). Then the map's remaining S1 rows (`S1-GLOBAL-ZEROS`, `S1-GROWTH`),
before any S2 item. Do **not** reopen a parked route (`ROUTE_TRIAGE.md` parked
all three with proofs of RH-equivalence of their day-45 bars); route work
resumes only with a genuinely new mechanism, recorded as a dated decision.

## 3. Non-negotiable protocol (unchanged)

- Green build ⇒ every built theorem fully proved. No `sorry`/`admit`, no new
  axioms, no weakening. Per-row axiom base stays `standard`.
- Frozen documents get dated addenda, never in-place edits.
- Built statements must stay character-identical to their frozen contracts.
- Generated views only via scripts; run the gate set locally before commit:
  `gen_status.py`, `export_agent_bundle.py --manifest`,
  `check_generated_fixpoint.py`, `check_repo_artifacts.py` (MANIFEST staleness
  has already broken CI once — regenerate after touching referenced files).
- Ledger gates: `gen_researchos_registry.py`, `check_axioms.py` (per-row),
  `check_ledger_isolation.py` (lane membership by registry, not namespace).
- `tasks/RIEMANN_HYPOTHESIS.md`: exactly one ACTIVE task, enforced by
  `gen_status.py`.
- All changes via PR to `main`; merge only with independent gates green.
- No AI-attribution footers or session URLs in commits/PR bodies (owner
  instruction).
- No claim of progress on the truth of RH. Every artifact states its claim
  boundary.

## 4. Known pitfalls (paid for once already)

- `bundles/MANIFEST.json` staleness fails `docs-sync` — regenerate, always.
- The isolation gate uses registry membership; root-namespace RH theorems are
  correct per the frozen bridge contract (S0_TRUST_DESIGN.md §4.6 amendment).
- `riemannZeta_ne_zero_of_one_le_re` has a strict-implicit `⦃s⦄` binder.
- At this pin `tsum` carries a `SummationFilter`; `∑'` = `unconditional`.
- Casts through `Int.toNat` broke bridge P1-d once; the audit-recorded
  explicit `Int.cast_natCast`/`Int.toNat_of_nonneg` chain is the repair shape.
- If the branch and a reviewer push concurrently: fetch + rebase, never force
  over the reviewer's work.
