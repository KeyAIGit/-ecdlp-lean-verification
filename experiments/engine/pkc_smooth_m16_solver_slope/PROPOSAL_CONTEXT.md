# Proposal provenance context

Proposal: `HGP-M16-SOLVER-SLOPE-001`

Generator session: `hyp-select-003-root-20260730`

Canonical generation instruction:

> Follow the KeyAI ECDLP Hypothesis Selection Constitution v3.0. Formulate
> and adversarially screen HYP-M16-SOLVER-SLOPE-001 as a source-faithful,
> plain-single-target proposal with one fixed synthetic DLP target Q and
> fresh committed relation points R_j. Include exact relation and recovery
> semantics, source-regime feasibility, separated causal controls, common
> cost accounting, scaling, death and promotion criteria. Do not execute a
> solver, rerun TASK-026, target secp256k1, alter execution gates, or claim
> global novelty.

The proposal `prompt_sha256` is the SHA-256 of the UTF-8 bytes inside the
blockquote after removing the Markdown quote prefixes and joining the lines
with one ASCII space.

The proposal `context_sha256` is the SHA-256 of the UTF-8 string formed by
sorting its `evidence_manifest` by path and joining
`path + ":" + sha256` records with a newline.

The proposal baseline `implementation_digest` is the SHA-256 of the literal
UTF-8 sentinel
`UNIMPLEMENTED-DESIGN-ONLY:separated-literal-coset-root-histogram-dag-and-cross-family-controls`.
It does not identify executable bytes. The `baseline_id` and proposal claim
boundary repeat that fact so the design cannot be mistaken for an
implementation binding; any future authorization must replace both.

The provenance `source_commit` is the protected-main audit base. This file
documents generation provenance on the proposal branch; it is not evidence
for the mathematical mechanism and is intentionally absent from the
source-commit evidence manifest.
