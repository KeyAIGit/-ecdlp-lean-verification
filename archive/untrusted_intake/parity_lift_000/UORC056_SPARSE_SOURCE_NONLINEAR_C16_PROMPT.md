# GPT Pro focused continuation

## SPARSE-SOURCE-NONLINEAR-ANTIRESOLVENT-066

Work only on the UORC056 endpoint-gauge branch. Reproduce C9-C15 before extending them. Use only frozen toy curves, known toy scalars, public generator replacements, and public secp256k1 constants. Do not accept an unknown-scalar external point, wallet, or production key.

## Central target

Let `T` be translation by one marked-generator step on functions/divisor coefficients. C15 proves that the endpoint-gauge valuation `z` satisfies

```text
(I+T)z=w,
```

where `w` has the seven public sources

```text
delta_0 + delta_(a-2) - delta_a
-delta_(m-1) - delta_m - delta_(n-2) + 2 delta_(n-1).
```

It also proves

```text
2z=sum_(j=0)^(n-1)(-T)^j w,
```

and that `z` plus one four-point public correction is the exact alternating parity divisor.

Compute the regularized local value/valuation at `Q=[k]G` with complete charged cost

```text
C_preprocessing+C_advice+C_memory+C_representation+C_online
=O(n^(1/2-epsilon))
```

for one fixed `epsilon>0`.

## Do not repeat as progress

```text
1. the symbolic geometric-series inverse;
2. a binary operator SLP whose apply step branches to all n shifts;
3. a translation-equivariant finite-field linear state;
4. a full Fourier or theta state;
5. a local-neighbour evaluator needing (n-1)/2 shifted inputs;
6. complete GLV orbit aggregates plus bounded corrections;
7. a rectangular baby/giant method at soft-O(sqrt(n));
8. a high-index Ward formula that returns the two public lift branches;
9. a field-valued generalized-Jacobian coordinate without a charged gauge.
```

## Mandatory attack order

### A. Apply/evaluate complexity of divide-and-conquer resolvents

Write exact recurrences for

```text
S_L(T)=sum_(j=0)^(L-1)(-T)^j.
```

Distinguish operator-description size from the cost of applying `S_L` to seven deltas and evaluating one encoded point. Search for shared subexpressions that avoid the natural recurrence `A(2L)=2A(L)+O(1)`. If impossible in a declared grammar, prove the leaf/input lower bound.

### B. Nonlinear transposed modular composition

Represent translation on the curve/Kummer function algebra without an `n`-dimensional regular state. Test whether one coordinate of `S_n(T)w` can be obtained by transposed modular composition, multipoint evaluation duality, or a residue pairing with state `o(sqrt(n))`. Charge construction of the modulus, branch, coefficients, and evaluation functional.

### C. Addition-enabled section circuit

Work directly with

```text
div(h_G)=E_G
```

and allow addition/subtraction of rational sections, derivatives, residues, and intersections. A candidate must output `h_G(Q)/h_G(Q+G)` or its local valuation, not a dense numerator/denominator.

### D. Structured ABP or determinant

Search for a bounded-width algebraic branching program, displacement-structured determinant, or first variation that creates the seven-source inverse without becoming a linear translation representation. Prove exact generator covariance and all-in cost.

### E. Scoped lower bounds

If no construction survives, declare one explicit grammar and prove a square-root or stronger bound. Useful grammars include bounded-branch resolvent trees, finite collections of shifted local rational functions, bounded-width ABPs with shift-labelled edges, and transposed product trees with charged state support.

## Positive completion gate

```text
exact output for every nonzero Q in H,
regularized handling at all divisor points,
generator covariance under G -> [u]G,
complete sub-square-root cost,
no hidden row index, branch seed, dense idempotent, or sqrt advice,
independent frozen replay.
```

## Required deliverables

```text
1. theorem-first memo;
2. deterministic replay and JSON;
3. full cost ledger;
4. explicit mechanism classification;
5. strongest successor;
6. no parity-oracle claim unless all gates pass.
```
