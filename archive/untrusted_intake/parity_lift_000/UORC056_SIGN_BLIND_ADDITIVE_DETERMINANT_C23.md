# UNIFORM-ORIENTED-ROOT-CIRCUIT-056

## C23: sign-blind additive, determinant, and resultant boundary

Date: 2026-08-14

Status: **C22's divisor-support union theorem does not extend directly to addition, because addition can create new zeros. C23 replaces that invalid extension with a different exact invariant. If every public leaf has the same value in the two global branch worlds `R` and `-R`, then every rational arithmetic circuit built from those leaves, including arbitrary addition and subtraction, has the same output in the two worlds. The same holds for determinants whose entries are sign-blind circuits and for Sylvester resultants whose coefficients are sign-blind. This theorem is independent of circuit size. A determinant or resultant can transport a branch-sensitive entry or coefficient, but it cannot manufacture one. No new public branch-sensitive leaf, compact branch-odd evaluator, parity oracle, or sub-square-root ECDLP algorithm is found.**

Only the public seven-curve corpus, six public generator replacements, and public secp256k1 constants are used. No external point with unknown scalar, wallet, private key, or production target is accepted.

The exact replay digest is:

```text
32211b2aa4de10648fa066571a4d389b72736e42682d69f246c1709d4191b251
```

The targeted GitHub Actions run `31846332776` completed successfully, including deterministic replay, exact coverage assertions, all three Lean files, and artifact upload.

## 1. Why C22 is not enough

C22 works with a valuation-transparent multiplicative grammar. At divisor level, multiplication, division, powers, and pullbacks add, subtract, scale, or permute divisor vectors. Therefore the support of the output is contained in the union of the charged leaf supports.

Addition behaves differently. Two nonzero functions can cancel at a new point:

```text
f(P) != 0,
g(P) != 0,
f(P)-g(P)=0.
```

Thus an addition-enabled circuit may create zeros not present in the supports of its leaves. It would be incorrect to claim that the C22 support-union theorem automatically covers addition, determinants, or resultants.

The C23 replay includes an exact witness on every retained curve:

```text
u=R(y)^2 != 0,
c=u != 0,
u-c=0,
```

and the same zero occurs after the global branch replacement `R -> -R` because `(-R)^2=R^2`.

The relevant invariant for addition is therefore not divisor support. It is two-world branch indistinguishability.

## 2. Two branch worlds

At any field value where the norm-one twist is finite and nonzero, consider two mathematical worlds:

```text
W+ : branch value z=R(y),
W- : branch value -z=-R(y).
```

The curve, generator, public field argument, subgroup order, compact norm, public coordinates, and every declared sign-blind atom are identical in the two worlds.

A leaf family `a_i` is sign-blind when

```text
a_i(-z)=a_i(z)
```

for every atom and every admissible nonzero branch value.

Examples include:

```text
public curve and point coordinates,
public constants,
z^2,
z^-2,
z^2+z^-2,
polynomial or rational expressions in z^2,
branch-even derivatives or norm data,
preprocessing state computed only from the same sign-blind data.
```

A value of `z`, one sign of a square root, one half-divisor coefficient, one oriented-root evaluation, or any advice correlated with the choice between `z` and `-z` is not sign-blind. It is charged as the missing branch-sensitive input.

The global branch flip in this package is distinct from the geometric involution

```text
tau(y)=-y.
```

For the Hilbert-90 twist one has `tau(R)=R^-1`, while the unresolved constant ambiguity is `R -> -R`. C23 concerns the latter.

## 3. Rational-circuit theorem

Let an arithmetic circuit be generated by:

```text
constants,
sign-blind atoms,
negation,
addition,
subtraction,
multiplication,
division,
inversion,
integer powers.
```

If every atom agrees in `W+` and `W-`, structural induction gives

```text
boxed:
Circuit(-z)=Circuit(z).                           (C23.1)
```

This is an equality theorem, not a complexity estimate. It holds for arbitrary finite width, depth, fan-out, repeated squaring, nested fractions, and cancellation patterns, provided the same deterministic field operations are applied to equal leaves.

Let the desired target take different values in the two worlds:

```text
Target(z) != Target(-z).
```

Then no output satisfying `(C23.1)` can equal the target in both worlds:

```text
boxed:
not [Circuit(z)=Target(z)
     and Circuit(-z)=Target(-z)].                 (C23.2)
```

For direct branch recovery, `Target(w)=w`. Therefore a sign-blind circuit cannot recover both `z` and `-z` when they are distinct.

## 4. Determinant corollary

Let every matrix entry be a sign-blind rational circuit:

```text
A_ij(-z)=A_ij(z).
```

Then the entire matrices agree entrywise:

```text
A(-z)=A(z).
```

Consequently:

```text
boxed:
det A(-z)=det A(z).                              (C23.3)
```

The determinant dimension may depend on `n`, and the internal determinant algorithm may use arbitrary addition and multiplication. The conclusion remains exact because identical matrices have identical determinants.

Therefore a compressed determinant can select the branch only if at least one of the following is already branch-sensitive:

```text
an entry,
a row or column normalization,
a coefficient-generation oracle,
preprocessing state,
an elimination pivot rule receiving branch-sensitive data,
an external advice bit.
```

The determinant can aggregate or transport that datum, but it does not generate it from sign-blind entries.

This complements the earlier packages:

```text
FROBENIUS-STICKELBERGER-DETERMINANT-050
INDEPENDENT-THETA-ROW-NORMALIZATION-051
```

The first shows that standard common-basis elliptic determinants factor into multiplicative net ratios. The second shows that scalar row trivializations contribute only the product of their row factors. C23 is independent of those factorizations and applies to every determinant whose complete entry generator is branch-blind.

## 5. Sylvester resultant corollary

A polynomial resultant is the determinant of its Sylvester matrix. Suppose

```text
F_z(T)=sum_i f_i(z)T^i,
G_z(T)=sum_j g_j(z)T^j,
```

and every coefficient is sign-blind:

```text
f_i(-z)=f_i(z),
g_j(-z)=g_j(z).
```

Then the Sylvester matrices are identical, hence

```text
boxed:
Res_T(F_-z,G_-z)=Res_T(F_z,G_z).                 (C23.4)
```

This closes resultants built solely from:

```text
compact norm data,
branch-even point functions,
polynomials in R^2,
branch-even derivatives,
sign-blind preprocessing,
public constants and coordinates.
```

It does not close a resultant containing a genuinely non-fixed coefficient. The replay verifies the exact distinction:

```text
sign-blind coefficients -> identical resultants,
coefficient z inserted explicitly -> resultant transports z.
```

For example:

```text
Res_T(T-z,T)=z.
```

This is not a construction of `z`; it places the desired branch directly into a coefficient.

## 6. Exact replay domain and coverage

The norm-one twist has zeros and poles at marked subgroup values. Ordinary field evaluation is therefore not defined or is zero at many of the marked values. The finite C23 replay does not pretend otherwise.

It reconstructs the C22 Hilbert-90 twist on:

```text
7 public frozen curves,
6 public generator replacements,
```

and evaluates the complete public `y`-line away from the zero and pole set of `R`. This gives 667 valid pairs of finite nonzero values

```text
R(y), R(-y)
```

satisfying

```text
R(y)R(-y)=1.
```

The exact per-instance counts are:

```text
base curves:             33, 41, 41, 57, 71, 85, 117
generator replacements: 33, 33, 33, 41, 41, 41
aggregate:               667
```

Across those values the replay performs:

```text
7327 rational-circuit equality checks,
2001 determinant equality checks,
667 Sylvester-resultant equality checks,
9995 exact two-branch decoder rejections.
```

For every valid `y` value it checks:

1. `R(y)R(-y)=1`;
2. the two global branches `z` and `-z` are distinct;
3. a declared library of nested additive, multiplicative, inverse, power, and rational expressions in sign-blind atoms has identical values on both branches;
4. 2 by 2, 3 by 3, and 4 by 4 sign-blind matrices have identical determinants;
5. sign-blind cubic-quadratic Sylvester pairs have identical resultants;
6. none of those equal-output objects recovers both branch targets;
7. addition creates a new zero from two nonzero sign-blind leaves;
8. a matrix or resultant supplied directly with the non-fixed leaf `z` transports the branch, demonstrating the exact boundary rather than claiming determinants are intrinsically branch-even.

The finite replay is a sanity check over concrete function values. The rational-circuit and determinant conclusions are proved abstractly in Lean and do not depend on the finite screen or on subgroup enumeration.

## 7. Lean formalization

The new file

```text
Ecdlp/Proved/Uorc056SignBlindCircuit.lean
```

formalizes:

```text
a typed rational-circuit syntax,
evaluation under an atom assignment,
equality of outputs under equal atom assignments,
closure of sign-blindness under arbitrary rational arithmetic,
the two-world separated-target obstruction,
entrywise sign-blind matrix equality,
sign-blind determinant equality,
determinant inability to match two separated target branches.
```

A Sylvester resultant is covered as a determinant whenever its coefficient circuits are sign-blind. The file does not formalize polynomial Sylvester construction itself because the determinant theorem already isolates the required hypothesis at matrix-entry level.

The targeted CI checks:

```text
Ecdlp/Proved/Uorc056NegationPaired.lean
Ecdlp/Proved/Uorc056Hilbert90Divisor.lean
Ecdlp/Proved/Uorc056SignBlindCircuit.lean
```

All three passed on Lean 4.31.0 with the pinned Mathlib revision.

## 8. Cost-gate consequence

The theorem is stronger than a linear representation lower bound inside its declared scope. No amount of additional sign-blind computation changes the branch information content:

```text
C_preprocessing arbitrary,
C_memory arbitrary,
C_representation arbitrary,
C_online arbitrary,
```

still yields identical outputs in the two branch worlds if all stored and queried data are sign-blind.

Therefore a positive candidate must exhibit at least one branch-sensitive resource. That resource must be charged in the complete ledger:

```text
how it is constructed,
its representation size,
its preprocessing cost,
its memory cost,
its query cost,
its generator covariance,
its behavior under R -> -R,
its behavior under G -> -G and G -> [u]G.
```

Calling the resource a determinant coefficient, pivot, oracle, analytic-continuation value, or normalization constant does not make it free.

## 9. What C23 closes

Closed:

```text
all finite rational circuits over sign-blind leaves,
addition and subtraction over compact norm-only data,
matrices whose entries are sign-blind circuits,
determinants of those matrices,
Sylvester resultants with sign-blind coefficients,
preprocessing and advice that are themselves identical in both branch worlds,
branch-even derivative and residue combinations used only through rational arithmetic.
```

The closure is independent of circuit size.

## 10. What remains open

Not closed:

1. a genuinely public branch-sensitive leaf with no hidden orientation advice;
2. twisted theta characteristics or distinct line bundles whose normalized section values have a proved nontrivial global branch law;
3. p-adic or analytic continuation with an independently public path and branch normalization;
4. determinants or resultants containing non-fixed coefficients that can themselves be generated below the cost gate;
5. addition-enabled elimination that derives a branch-sensitive coefficient from public `E,G,Q` by a mechanism not factoring through sign-blind data;
6. a public nonlocal jump law propagating the canonical infinity normalization to the query;
7. unrestricted circuits whose leaves include a new branch-odd anchor.

Thus the remaining question is no longer whether addition or a determinant can create orientation from nothing. It is:

```text
Which public primitive, if any, is genuinely different in the R and -R worlds,
and can that primitive be constructed with total cost O(n^(1/2-epsilon))?
```

## 11. Final flags

```text
addition_can_create_new_zeros=true
C22_support_union_extended_to_addition=false
sign_blind_rational_circuit_boundary_proved=true
sign_blind_determinant_boundary_proved=true
sign_blind_sylvester_resultant_boundary_proved=true
determinant_or_resultant_can_transport_nonfixed_leaf=true
determinant_or_resultant_can_manufacture_nonfixed_leaf=false
new_branch_sensitive_leaf_found=false
general_addition_enabled_circuit_blocked=false
compressed_nonfixed_resultant_or_determinant_open=true
compact_branch_odd_evaluator_found=false
sub_sqrt_evaluator_found=false
parity_oracle_found=false
sub_sqrt_ecdlp_found=false
```

## 12. Successor

The successor is

```text
BRANCH-SENSITIVE-LEAF-CLASSIFICATION-073.
```

Its first obligation is to classify every surviving proposed coefficient or section by its exact transformation under the global branch flip. A candidate that is sign-blind is immediately covered by C23. A candidate that is branch-sensitive must provide a public construction and complete cost ledger without receiving an oriented root, half-divisor, unknown scalar index, or equivalent advice.
