# GPT Pro focused continuation

## NONLINEAR-PUBLIC-Q-ALGEBRAIC-STATE-077

Start from `research/uorc056-public-q-resultant-c27`. Create a separate branch
named `research/uorc056-nonlinear-public-q-state-c28`. Do not change `main`,
`research/parity-lift-000`, or the C23-C27 branches.

Use only symbolic variables, public frozen toy curves, public prime orders,
public secp256k1 constants, and deterministic public coefficient families. Do
not accept an external unknown-scalar point, wallet, private key, production
target, numeric `k`, its bits, a dual-character table, an oriented-root table,
a half-orbit label table, or target-dependent advice.

## Fixed input

The central target remains

```text
Q=[k]G,
A(E,G,Q)=Y_G(x(Q))/y(Q)=(-1)^k,
```

with complete charged cost

```text
C_preprocessing + C_advice + C_memory
+ C_representation + C_online
=O(n^(1/2-epsilon)).
```

C23 proves that arbitrary rational circuits, determinants, and resultants over
sign-blind leaves remain sign-blind. C24 finds no public compact
branch-sensitive leaf. C25-C26 close broad sparse determinant/resultant
symmetry classes and separate numeric-`k` from public-Q control. C27 proves:

```text
nontrivial base-field linear quotient dimension >= ord_n(p),
secp256k1 ord_n(p)=(n-1)/6,
sparse trace atoms >= (n-1)/2,
coordinate-sparse Krylov cross-support >= (n-1)/2.
```

Thus the next candidate must be genuinely nonlinear and cannot merely hide a
linear character, regular-representation vector, trace dictionary, or
coordinate-support table.

## Central target

Construct or exclude, in an exact declared grammar, a state

```text
S_G(Q) in F_p^d or on a public algebraic variety V,
d=O(1) or polylog(n),
```

with a decoder

```text
Decode(E,G,Q,S_G(Q))=(-1)^k.
```

The state and decoder must be public-Q algorithms. Their descriptions may have
large algebraic degree only if that degree is generated by a genuinely short
uniform circuit whose complete construction and evaluation costs are charged.

## First candidate

The first concrete state is the CM residue pair

```text
S_G(Q)=(A(T),B(T)),
T=x(Q)^3,
Y_G(X)=A(X^3)+X B(X^3)+X^2 C(X^3).
```

The existing direct decoder is

```text
(-1)^k
=
(2A(T)^2+2A(T)xB(T)-x^2B(T)^2)/(2yA(T))
```

on the regular domain. Do not build dense coefficient arrays for `A` or `B`.
The question is whether their values at one public `T` admit a short joint
recurrence, modular-composition chain, norm/trace tower, or algebraic state
transition.

## Mandatory attack order

### A. Exact state grammar

For every candidate, define:

```text
state dimension,
base field or extension field,
initial public seed,
transition/evaluation maps,
decoder,
exceptional denominator set,
generator covariance,
branch transformation law.
```

Reject a candidate whose initial seed already contains an oriented root,
nonlocal branch value, unknown scalar index, or equivalent advice.

### B. Low-degree algebraic composition boundary

Let

```text
S:E -> V,
D:V x E -> P^1
```

be rational maps and put `F(Q)=D(S(Q),Q)`. Prove and formalize the exact divisor
statement:

```text
if F([k]G)=(-1)^k for every 1<=k<n,
then F^2-1 has at least n-1 marked zeros unless it vanishes identically.
```

Transfer this to a lower bound on the pole degree of `F`, and then to a product
bound involving the algebraic degrees of `S` and `D` in each declared state
model. State clearly that degree alone yields only logarithmic straight-line
program lower bounds and does not close high-degree low-size circuits.

### C. Translation-defect equation

Canonical parity satisfies

```text
sigma(P+G)=-sigma(P)
```

on every marked edge except the unique wrap edge. For a candidate decoder
`F`, study

```text
Delta_F(P)=F(P+G)+F(P).
```

It vanishes on `n-2` marked points. Derive exact divisor or degree consequences
for rational functions, rational state decoders, and bounded-degree algebraic
correspondences. Treat the wrap point and all poles explicitly.

### D. Joint A/B recurrence search

Derive exact relations for the CM components under:

```text
Q -> Q+G,
Q -> [2]Q,
Q -> alpha Q,
T -> L_b(T)=-(T+4b)^3/(27T^2),
```

and under all existing Hilbert-90/Miller cocycle identities. Search for a
closed update rule on a bounded tuple such as

```text
(A(T),B(T),A(L_b(T)),B(L_b(T)),public auxiliaries).
```

Every rule must be tested for whether it merely transports an unknown branch
seed.

### E. Modular composition

Test representations of `A` and `B` as compositions

```text
F_0 o F_1 o ... o F_r
```

or as a straight-line program in public rational maps. Charge:

```text
construction of every F_i,
storage of all coefficients,
branch data,
field extensions,
composition and evaluation costs.
```

A high-degree object counts as compact only when its complete uniform program
is compact.

### F. Algebraic-state collision and branch audit

Use the C23-C24 two-world method. Determine whether every candidate leaf and
state coordinate is equal under the unresolved global branch flip. If so,
close the complete rational decoder class over that state. If not, identify
exactly how the differing coordinate is publicly constructed and charge it.

### G. All-point replay

Any positive candidate must receive only `(E,G,Q)` and pass:

```text
all nonzero scalars on every frozen toy curve,
all public marked-generator replacements,
G -> -G,
G -> [u]G,
Q -> -Q,
three GLV rotations,
all denominator zeros and degree drops.
```

The harness may know `k` only for scoring. It may not drive control flow.

### H. Complete cost gate

Charge:

```text
preprocessing,
advice,
state dimension,
state-map construction,
coefficient generation,
representation,
memory,
online field operations,
branch choice,
exception handling,
verification repetitions.
```

### I. Formalization

Formalize exact ring identities, divisor/zero-count implications, degree
transfers, state-collision theorems, and fixed secp256k1 arithmetic in Lean.
Do not label a finite screen, heuristic circuit search, or unproved complexity
claim as kernel-checked.

## Positive gate

A positive result must include:

```text
one exact public-Q state constructor,
one exact decoder,
no numeric-k or branch-seed leakage,
all-point correctness,
generator covariance,
complete sub-square-root cost,
reproducible code,
held-out validation.
```

## Negative gate

A negative result must include:

```text
one exact nonlinear state grammar,
a proved collision, divisor, degree, orbit, or query lower bound,
complete exceptional-set treatment,
classes covered,
classes not covered.
```

Do not claim an unrestricted arithmetic-circuit, parity, or ECDLP lower bound.

## Required final flags

```text
bounded_dimensional_nonlinear_state_found=?
public_branch_sensitive_seed_found=?
joint_A_B_recurrence_found=?
modular_composition_state_found=?
translation_defect_bound_proved=?
low_degree_algebraic_state_blocked=?
high_degree_low_size_state_blocked=?
all_point_public_Q_replay_passed=?
exact_parity_extraction_found=?
exact_Hilbert90_branch_bridge_found=?
complete_cost_gate_passed=?
compact_branch_odd_evaluator_found=?
sub_sqrt_evaluator_found=?
parity_oracle_found=?
sub_sqrt_ecdlp_found=?
```
