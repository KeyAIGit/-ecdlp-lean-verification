name: Verify Lean proofs

# Triggers: every push, every pull request, manual run, and a daily re-check.
on:
  push:
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * *'   # daily at 06:00 UTC: re-verify the whole base

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Featherless API smoke test (non-blocking)
        continue-on-error: true
        env:
          FEATHERLESS_API_KEY: ${{ secrets.FEATHERLESS_API_KEY }}
        run: |
          if [ -z "$FEATHERLESS_API_KEY" ]; then
            echo "FEATHERLESS_API_KEY is missing or not available to this workflow."
            exit 0
          fi
          echo "FEATHERLESS_API_KEY is present. Value is hidden."
          status=$(curl -sS -o featherless_models.json -w "%{http_code}" \
            "https://api.featherless.ai/v1/models?available_on_current_plan=true" \
            -H "Authorization: Bearer ${FEATHERLESS_API_KEY}")
          echo "Featherless HTTP status: ${status}"
          if [ "$status" != "200" ]; then
            echo "Featherless API response:"
            cat featherless_models.json
            exit 0
          fi
          python3 - <<'PY'
          import json
          with open('featherless_models.json', 'r', encoding='utf-8') as f:
              payload = json.load(f)
          ids = [m.get('id') for m in payload.get('data', []) if isinstance(m, dict) and m.get('id')]
          print(f"Featherless available model count: {len(ids)}")
          for name in ["Pythagoras-Prover-4B", "Goedel-Prover-V2-32B"]:
              matches = [x for x in ids if name.lower() in x.lower()]
              print(f"{name}: {'FOUND ' + str(matches[:3]) if matches else 'not found in current-plan list'}")
          PY

      # Fast, cheap gate: fail before building if any proof is left incomplete.
      - name: Ensure no incomplete proofs remain
        run: |
          if grep -rniI 'sorry' Ecdlp/; then
            echo "::error::Found 'sorry' in Ecdlp/ - a proof is incomplete"
            exit 1
          fi
          echo "All proofs complete: no 'sorry' in Ecdlp/"

      - name: Install elan (Lean toolchain manager)
        run: |
          curl -fsSL https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh \
            | sh -s -- -y --default-toolchain none
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"

      - name: Fetch prebuilt Mathlib cache
        run: lake exe cache get

      - name: Build and verify ALL proofs
        run: lake build

      - name: Prover target attempt (artifact-only, non-blocking)
        continue-on-error: true
        env:
          FEATHERLESS_API_KEY: ${{ secrets.FEATHERLESS_API_KEY }}
        run: |
          python3 scripts/prover_target_attempt.py \
            --target zmod_from_mod_zero \
            --fast-attempts 4 \
            --heavy-attempts 2

      - name: Add prover target report to summary
        if: always()
        run: |
          echo "## Prover target attempt" >> "$GITHUB_STEP_SUMMARY"
          if [ -f prover-target-report.md ]; then
            cat prover-target-report.md >> "$GITHUB_STEP_SUMMARY"
          else
            echo "No prover target report was produced." >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload prover target report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: prover-target-attempt-report
          path: |
            prover-target-report.md
            prover-target-success.lean
            ProverTargetAttempt.lean
          if-no-files-found: ignore
