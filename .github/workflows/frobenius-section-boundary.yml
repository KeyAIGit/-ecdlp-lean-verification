name: Frobenius section boundary

on:
  push:
    branches:
      - research/parity-lift-000
    paths:
      - Ecdlp/Proved/FrobeniusSectionBoundary.lean
      - .github/workflows/frobenius-section-boundary.yml
  workflow_dispatch:

concurrency:
  group: frobenius-section-boundary-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4

      - name: Reject incomplete proof
        run: |
          if grep -nI 'sorry' Ecdlp/Proved/FrobeniusSectionBoundary.lean; then
            echo "::error::Frobenius section boundary contains sorry"
            exit 1
          fi

      - name: Install elan
        run: |
          curl -fsSL https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh \
            | sh -s -- -y --default-toolchain none
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"

      - name: Fetch Mathlib cache
        run: lake exe cache get

      - name: Kernel-check boundary
        run: lake build Ecdlp.Proved.FrobeniusSectionBoundary
