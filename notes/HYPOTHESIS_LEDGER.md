# Hypothesis ledger (auto-appended; see notes/HYPOTHESIS_EXPLORER.md)

## `sig:15755048edeb` — axis: geometric (higher genus, abelian varieties, moduli, covers) — outcome: **refuted**
- **Hypothesis:** Consider the family of genus-2 curves C_a that arise as (2,2)-covers gluing secp256k1's E to a *twist family* E_a via a fixed 2-torsion identification, so that Jac(C_a) is (2,2)-isogenous to E x E_a. Because secp256k1 has trivial rational 2-torsion structure over F_p except its single rational 2-torsion point (x^3+7 has exactly one root in F_p), the gluing data is rigid: there is essentially a *one-parameter* pencil of such genus-2 gluings indexed by the choice of E_a sharing E's unique F_p-rational 2-torsion x-coordinate. The angle is to study whether the *Prym/Kummer geometry* of this pencil forces a nontrivial relation among the Frobenius eigenvalues of E and E_a that is detectable purely from the discriminant locus of the (2,2)-cover — i.e., whether the moduli position of Jac(C_a) inside A_2 constrains the group order #E(F_p) via the vanishing of an Igusa-invariant combination evaluated at the gluing point.
- **Sub-claim:** Isolable first test of the (2,2)-gluing angle: (A) For secp256k1's p = 2^256-2^32-977, the polynomial x^3+7 has EXACTLY ONE root in F_p (so E has a unique rational 2-torsion x-coordinate), and gcd(x^3+7, 3x^2) = 1 in F_p[x] certifying the 2-torsion locus is smooth (E nonsingular). (B) Concrete arithmetic verification of the (2,2)-cover decomposition mechanism on a toy prime p0=101: y^2=x^3+7 over F_101 likewise has exactly one rational 2-torsion x-coordinate, and for a genus-2 curve C whose Jacobian is (2,2)-isogenous to E x E_a we have the exact multiplicative constraint #Jac(C)(F_101) = #E(F_101) * #E_a(F_101). We build the isogeny factors explicitly by enumerating ALL elliptic curves y^2=x^3+a3*x+a1 sharing E's rational 2-torsion x-coordinate x0 (i.e. curves with x0 as a root of their cubic), compute each #E_a(F_101) by point counting, and verify: (i) E itself is in this pencil, (ii) the pencil is a genuine 1-parameter family (>1 member), (iii) fixing #E(F_101) together with a target product value pins #E_a(F_101) to the finite explicit set of pencil members realizing that product (the resultant/degree-count claim, done here by exact enumeration). All computed as exact integers mod 101; the product #E*#E_a is verified to equal the Weil-consistent Jacobian order candidate #Jac = (#E)*(#E_a) which by the Frobenius eigenvalue relation for E x E_a equals the exact integer product.
- **Evidence:** `== 1, f'expected exactly one rational 2-torsion x-coord, got {len(distinct)}'
           ^^^^^^^^^^^^^^^^^^
AssertionError: expected exactly one rational 2-torsion x-coord, got 0`

## `sig:2b541b9ec4a7` — axis: index-calculus / factor-base (Semaev summation, Weil descent, decomposition bases) — outcome: **rejected-by-gate** (gate 0/2)
- **Hypothesis:** The Semaev summation polynomials S_m for secp256k1 acquire a hidden low-degree factorization when restricted to the subvariety cut out by the p-power Frobenius congruence, because p = 2^256 - 2^32 - 977 has an unusually sparse binary/2-adic structure. Concretely: define a 'Frobenius-twisted decomposition base' as the set of x-coordinates lying in the fiber of the map x -> x^p mod (x^3+7-related relations), and conjecture that the third Semaev polynomial S_3(x1,x2,x3) becomes reducible modulo the ideal generated by the sparse-p relation R(x) = x - (x^{2^256} * x^{-2^32} * x^{-977})-type collapse, yielding a nontrivial factor of controlled degree in each variable. If such a factor exists, the summation-polynomial root search would split into a product of smaller systems, giving a genuine (if modest) structural edge tied specifically to the SPARSITY of secp256k1's p rather than to any generic prime-field bound.
- **Sub-claim:** For the third Semaev summation polynomial of y^2 = x^3 + 7, S_3(x1,x2,x3) = (x1-x2)^2 x3^2 - 2((x1+x2)(x1 x2 + b) + 2a? ...) --- concretely with a=0, b=7: S_3 = (x1*x2 - x3*(x1+x2))^2 - 4*(x3+x1+x2)*(x1*x2*x3 - 7)  (the standard summation polynomial). The isolable, exactly-checkable sub-claim is a FALSIFICATION test of the sparsity hypothesis: over a toy sparse pseudo-Mersenne prime p_s = 2^13 - 2^5 - 7 = 8153 (a 3-term prime, but note 8153 is NOT prime; we use the actual prime toy p_s = 8191 = 2^13-1 for the field and separately study the SPARSE reduction map) we compare, for the polynomial g(x3) = S_3(x1,x2,x3) at fixed generic x1,x2, the factorization over GF(p) of gcd(g(x3), x3^p - x3) for a SPARSE prime p and a NON-sparse (random dense-binary) prime p' of the same bit width. The precise checkable claim: the number and degrees of the irreducible factors of gcd(S_3(a,b,x3) mod p, x3^p - x3) is governed ONLY by which of the two candidate x3-roots (the two solutions making three points sum to O) lie in GF(p), and is INDEPENDENT of the binary sparsity of p. We verify exactly that for two same-bit primes (one 3-term sparse: p1 = 2^7 - 2^3 - 1 = 119? not prime; use p1 = 8191 = 2^13-1 vs p2 = 7919 a dense 13-bit prime), the degree pattern of the gcd-of-Semaev-with-Frobenius matches the pure root-counting prediction identically, so no EXTRA sparse-p common factor appears — falsifying the hypothesis that sparsity introduces a hidden low-degree factor. Sub-claim as tested: (i) S_3 satisfies the exact symmetric identity S_3(x1,x2,x3)=S_3 under any permutation of variables; (ii) for fixed generic a,b in GF(p), gcd_{x3}(S_3(a,b,x3), x3^p - x3) equals exactly prod over the (at most 2) x3-roots of S_3(a,b,x3) that are squares-consistent members of GF(p) of (x3 - root), i.e. its degree equals the count of GF(p)-roots of the quadratic S_3(a,b,x3); (iii) this count/degree is identical between the sparse prime 8191 and the dense prime 7919 whenever both quadratics have the same number of GF(p)-roots for matched inputs, demonstrating no sparsity-specific extra factor.
- **Evidence:** `CERT_OK`

## `sig:1be281a65513` — axis: algebraic (endomorphism ring / CM, isogeny graph, torsion structure E[n]) — outcome: **refuted**
- **Hypothesis:** secp256k1 has CM by the order Z[omega] in the imaginary quadratic field Q(sqrt(-3)) (since p = 1 mod 3 and j-invariant = 0), giving the explicit degree-2 endomorphism zeta:(x,y)->(beta*x, y) with beta^3=1 mod p. The novel angle is to study the ARITHMETIC of the CM lattice as it acts on the l-torsion: for each odd prime l, the Frobenius pi and the CM endomorphism omega generate a rank-2 subring of End(E) whose reduction mod l governs how E[l] splits as a module over Z[omega]/l. Specifically, whether l splits, is inert, or ramifies in Z[omega] (determined by l mod 3) forces a rigid decomposition E[l] = L_1 (+) L_2 into two omega-eigen-lines when l splits, versus an irreducible 2-dim omega-module when l is inert. The hypothesis is that this omega-eigenline structure imposes an exact multiplicative relation between the two eigenvalues of Frobenius on E[l] and the cube root beta, constraining any DLP index-relation to respect the omega-grading — a constraint expressible as a polynomial congruence linking beta, the division polynomial psi_l, and the trace t.
- **Sub-claim:** For secp256k1 (p = 2^256 - 2^32 - 977, curve y^2 = x^3 + 7), let beta be a primitive cube root of unity mod p (so beta^3 = 1, beta != 1). Then: (a) beta^2 + beta + 1 = 0 mod p (exact congruence, minimal polynomial of the CM endomorphism zeta:(x,y)->(beta*x,y)); (b) the map zeta sends affine curve points to curve points because (beta*x)^3 + 7 = x^3 + 7 as a polynomial identity mod p (using beta^3=1), while for a non-cube-root c=2 the identity fails; (c) endomorphism-module test on l-torsion x-coordinates via the division polynomial psi_l: for the SPLIT prime l=7 (7 = 1 mod 3), Res_x(psi_7(x), psi_7(beta*x)) = 0 mod p, whereas for the INERT prime l=5 (5 = 2 mod 3), Res_x(psi_5(x), psi_5(beta*x)) != 0 mod p. This distinguishes the two Z[omega]/l module types (paired omega-eigenlines vs single irreducible 2-dim orbit) by an exact resultant certificate.
- **Evidence:** `py", line 141, in <module>
    assert R5 % p != 0, 'inert prime l=5 must give nonzero resultant'
           ^^^^^^^^^^^
AssertionError: inert prime l=5 must give nonzero resultant`

## `sig:e6b706b7d934` — axis: analytic / p-adic (formal groups, canonical heights, Frobenius / L-functions) — outcome: **rejected-by-gate** (gate 0/2)
- **Hypothesis:** The canonical (Neron-Tate) height pairing on secp256k1, when composed with the p-adic sigma-function expansion of the formal group at the identity, imposes a rigid congruence constraint linking the p-adic elliptic logarithm of a point P to the leading coefficients of the division polynomials psi_n reduced mod powers of p. Concretely: the formal-group logarithm log_E(z) = z + sum a_i z^i / i has coefficients a_i whose p-adic valuations are governed by the trace of Frobenius t, and I hypothesize that the 'anomaly defect' delta = (p + 1 - t) mod p^2 -- NOT just mod p, since the curve is non-anomalous mod p -- controls a SECOND-ORDER congruence in the formal group that partitions the x-coordinates of low-order-related points into predictable residue classes mod p^2. That is, even though E is not anomalous, a higher p-adic lift of Frobenius (the unit root of the characteristic polynomial T^2 - tT + p acting on the formal group) yields a canonical 'p-adic phase' phi_p(P) = log_E(x(P)/y(P)) whose valuation stratifies scalar multiples [k]P by v_p of their y-coordinate in a way computable from t alone, giving structure to the DLP map k -> [k]P that is invisible mod p.
- **Sub-claim:** For the elliptic curve E: y^2 = x^3 + 7 (secp256k1's Weierstrass model, symbolic over Q), the formal-group logarithm log_E(z) = sum_{i>=1} a_i z^i, obtained by integrating the invariant differential omega = dz/(1 + a1 z + ... ) expressed via the formal-group parameter z = -x/y, satisfies EXACTLY: (a) a_1 = 1; (b) for each i in 1..12, i*a_i is an integer (equivalently denom(a_i) divides i); and (c) since a1=a2=a3=a4=0 and a6=7 in this model, all even-index coefficients a_{2k} vanish and the first nonzero higher coefficient beyond a_1 appears at the index dictated by a6=7 (namely a_7 = -7/... with the exact rational computed here). This is a fully symbolic, p-independent exact rational identity, the necessary structural prerequisite of the angle. Additionally we verify the Frobenius arithmetic fact: for a small curve y^2=x^3+7 over F_101 with trace t and order N=102-t... i.e. that T^2 - tT + p has the two roots summing to t and multiplying to p, checked as an EXACT integer factorization identity u*(t-u)=p is generally false but the resultant/product-sum relation prod=p, sum=t holds exactly.
- **Evidence:** `CERT_OK`

