# Hypothesis ledger (auto-appended; see notes/HYPOTHESIS_EXPLORER.md)

## `sig:d9c7aab38e8b` — axis: analytic / p-adic (formal groups, canonical heights, Frobenius / L-functions) — outcome: **refuted**
- **Hypothesis:** The p-adic elliptic logarithm on secp256k1, built from the formal group law over Z_p, has coefficients whose p-adic valuations are governed by a rigid combinatorial pattern tied to the trace of Frobenius t. Specifically, I hypothesize that the formal-group logarithm log_F(T) = T + sum_{n>=2} (b_n/n) T^n, with b_n determined by the invariant differential expansion, satisfies a valuation recurrence in which the anomaly-detecting quantity (a_p - 1 mod p^k, where a_p = t) appears as a linear obstruction. The novel angle: rather than lifting a SINGLE point, study the ENTIRE family of division-polynomial reductions of the formal log modulo increasing powers of small torsion primes l (l=2,3,5,7) and ask whether the CROSS-VALUATIONS between the l-adic filtration of E[l^k] and the p-adic filtration of the formal group produce a nontrivial 'mixed height' pairing. If the two filtrations were rigidly correlated, the ECDLP scalar could be read off coordinate-wise from the joint (l,p)-adic expansion. The concrete claim is that they are provably DECORRELATED at first order but the decorrelation defect is an explicit polynomial in t.
- **Sub-claim:** For the curve E: y^2 = x^3 + 7 over Q, the formal group law logarithm log_F(T) = T + sum_{n>=2} (b_n/n) T^n, obtained by integrating the invariant differential omega = dx/(2y) expressed in the uniformizer T = -x/y (so omega = (1 + c_2 T^2 + c_3 T^3 + ...) dT and log_F is its formal antiderivative), has EXACT rational coefficients that satisfy the following precise, isolable facts verifiable by exact symbolic computation: (a) writing log_F(T) = sum_{m>=1} d_m T^m with d_1 = 1, the coefficients through degree 8 equal the exact rationals d_2=0, d_3=0, d_4=-7/4, d_5=0, d_6=0, d_7=0, d_8=-35/16 (i.e. all a_1..a_4,a_6 = 0 except a_6 = 7 for this short Weierstrass curve, forcing every d_m to vanish unless m ≡ 1 (mod 3)); (b) the only nonvanishing coefficients up to degree 13 occur exactly at m ≡ 1 (mod 3), matching the fact that the curve has a_4 = 0 so omega has a pure T^2-weighted expansion in powers of T^3; (c) the exponential exp_F(S) inverse of log_F satisfies exp_F(log_F(T)) = T + O(T^{14}) exactly as formal power series with rational coefficients — a nontrivial cancellation identity certifying log_F is a well-defined formal isomorphism. This is the first isolable test: it establishes the exact p-adic-independent skeleton of the formal-group log (the c_n/(n) structure) before any cross-valuation with l-torsion, and it pins down the a_6=7 dependence exactly.
- **Evidence:** `ile "/tmp/tmpwfqv3wxg/cert.py", line 53, in <module>
    assert d[m] == val, ('coeff mismatch', m, d[m], val)
           ^^^^^^^^^^^
AssertionError: ('coeff mismatch', 4, 0, -7/4)`

## `sig:19e945f326a3` — axis: algebraic (endomorphism ring / CM, isogeny graph, torsion structure E[n]) — outcome: **rejected-by-gate** (gate 0/2)
- **Hypothesis:** secp256k1 has CM discriminant D = t^2 - 4p (with t the Frobenius trace), and because p was chosen with a special low-Hamming-weight structure (p = 2^256 - 2^32 - 977), the ring Z[pi] generated by Frobenius may sit at a NON-MAXIMAL order inside the CM field K = Q(sqrt(D)), i.e. the conductor f of Z[pi] in O_K could be nontrivial. The novel angle: study whether the *j-invariant 0 special structure* (secp256k1 has j=0, so it possesses the extra order-3 automorphism [zeta_3] from the CM by Z[zeta_3], discriminant -3) creates an arithmetic bridge between the RATIONAL 3-torsion action and the Frobenius action. Concretely, on j=0 curves End(E) contains Z[omega] with omega^2+omega+1=0, and Frobenius pi factors relative to omega. The hypothesis is that pi, omega, and the [2]-multiplication satisfy a *fixed low-degree algebraic relation over the endomorphism ring* that constrains discrete logs mod 3: the omega-eigenspace decomposition of E[l] (for l = 1 mod 3, where omega acts with two distinct eigenvalues) splits the DLP into two half-instances whose relative offset is determined by a single explicit endomorphism-ring coefficient, not by generic group order.
- **Sub-claim:** For secp256k1 (j=0 curve y^2=x^3+7 over F_p), the CM structure by Z[omega] (omega^2+omega+1=0) is exactly realized: (a) there exists beta in F_p with beta != 1, beta^3 = 1 and beta^2+beta+1 = 0 mod p, giving the order-3 automorphism [omega]:(x,y)->(beta*x,y); (b) the Frobenius trace t of secp256k1 (t = p+1-n) admits an exact factorization pi = a+b*omega in Z[omega], i.e. there exist integers a,b with t = 2a-b and p = a^2-a*b+b^2 (the norm form of the maximal order Z[omega], discriminant -3), verified as exact integer identities using the true known n; (c) consequently the CM discriminant D = t^2-4p equals -3*(2b-a)^2... i.e. D = -3*g^2 for an explicit integer g, confirming K=Q(sqrt(-3)) and that Z[pi] sits inside O_K = Z[omega] with 4p-t^2 = 3*g^2.
- **Evidence:** `CERT_OK`

## `sig:8e1e17a09e8b` — axis: cross-domain analogy (lattices, codes, dynamical systems, tensor networks, category theory) — outcome: **refuted**
- **Hypothesis:** Model the recursive doubling tower of division polynomials {ψ_{2^k}(x)}_{k≥1} for secp256k1 as a matrix-product-state (tensor-network) representation: each ψ_{2^{k}} is expressed via the standard doubling recursion ψ_{2k} = ψ_k(ψ_{k+2}ψ_{k-1}^2 - ψ_{k-2}ψ_{k+1}^2)/(2y) as a contraction of small 'local' tensors (the coefficients of ψ_{k-1},ψ_k,ψ_{k+1},ψ_{k+2} in the fixed basis of monomials in x). The angle is to ask whether this doubling recursion admits a *low bond-dimension* tensor-network (MPS/MPO) factorization over F_p analogous to those used for transfer matrices in 1D quantum spin chains — i.e., whether the sequence of Sylvester/resultant matrices built from consecutive ψ's collapses to low rank mod p for the secp256k1 parameters, which would reveal hidden recursive redundancy in the doubling ladder not visible from the algebraic degree alone.
- **Sub-claim:** For secp256k1 (p = 2^256 - 2^32 - 977, curve y^2 = x^3 + 7), consider the division polynomials psi_k reduced to univariate polynomials in x by substituting y^2 = x^3 + 7 (so psi_1=1, psi_2=2y is handled by tracking the y-factor separately; we work with the standard normalized forms f_k where psi_{2m+1}=f_{2m+1} and psi_{2m}=2y*f_{2m}). For each k in {2,3,4,5,6} we build the doubling-ladder Sylvester matrix M_k, defined as the Sylvester matrix of the pair (psi_k, psi_{k+1}) treated as polynomials in x over GF(p). The precise checkable sub-claim is: for every k in {2,...,6}, rank(M_k mod p) EQUALS the generic full Sylvester rank deg(psi_k) + deg(psi_{k+1}), i.e. Res_x(psi_k, psi_{k+1}) != 0 mod p and no rank collapse occurs. This is the 'no-collapse' certificate (branch (a)): the consecutive division polynomials are exactly coprime mod p and the Sylvester/tensor-contraction matrix has full generic rank for all tested k, ruling out low-bond-dimension rank deficiency in the doubling ladder over the secp256k1 field for these small k.
- **Evidence:** `)
  File "/tmp/tmpwy_2n284/cert.py", line 76, in build
    assert poly.degree() <= 0, ('even parity fail', n)
           ^^^^^^^^^^^^^^^^^^
AssertionError: ('even parity fail', 6)`

## `sig:0a861678f1ea` — axis: geometric (higher genus, abelian varieties, moduli, covers) — outcome: **refuted**
- **Hypothesis:** Consider the Kummer-type genus-2 curves C whose Jacobians J(C) admit a (2,2)-isogeny cover map to E = secp256k1 (i.e. E appears as an optimal quotient of a genus-2 Jacobian, J(C) ~ E x E'). Rather than the standard Weil-descent direction (which builds a high-genus curve OVER a subfield and fails for prime fields), the angle is INWARD: catalog the finite set of genus-2 curves over F_p whose Jacobian splits with E as a factor, and study whether the complementary factor E' has arithmetic (its own trace t', its 2-torsion structure) that is forced into congruence relations with E's group order via the compatibility of the (2,2)-isogeny kernel. The novel claim is that the splitting-compatible E' are severely constrained: the resultant of E's 2-division polynomial with the 'gluing' polynomial that parametrizes (2,2)-splittings must vanish, pinning E' to a discrete lattice of admissible traces, and this lattice never contains a curve whose order shares a smooth cofactor with n = #E(F_p). I.e. the geometry of split-Jacobian gluing provably cannot manufacture a smooth-order companion for secp256k1.
- **Sub-claim:** For E: y^2 = x^3+7 over F_p (secp256k1 prime), the 2-division polynomial is psi_2(x) = x^3+7 itself (its roots are the nonzero x-coordinates of 2-torsion), and over F_p it has EXACTLY ONE root, so E[2](F_p) = Z/2 (a single rational 2-torsion point, the point at infinity plus one affine 2-torsion point). Consequently 4 | #E(F_p) is FALSE from the rational 2-torsion side; and for the (2,2)-gluing setup with a symbolic companion g(x)=x^3+a*x+b, the classical gluing compatibility resultant Res_x(f'(x), g'(x)) (f=x^3+7) is a NONTRIVIAL, non-identically-zero polynomial in (a,b): specifically Res_x(3x^2, 3x^2+a) = 9*a^2 (times a nonzero constant), which is a genuine constraint vanishing only on a<->0 lattice, confirming that split-Jacobian gluing imposes a real (non-vacuous) algebraic condition rather than being automatically satisfiable. This is the first isolable test: (a) exact E[2] structure over the true secp256k1 prime, (b) non-triviality of the gluing resultant.
- **Evidence:** `dule>
    assert num_roots == 1, ('expected 1 rational 2-torsion x-root, got %d' % num_roots)
           ^^^^^^^^^^^^^^
AssertionError: expected 1 rational 2-torsion x-root, got 0`

## `sig:ffd861cb5410` — axis: reduction / equivalence (to factoring, to other DLPs, to hidden-subgroup variants) — outcome: **rejected-by-gate** (gate 0/2)
- **Hypothesis:** The ECDLP on secp256k1 can be reduced to a *simultaneous* hidden-shift problem over the reduction map into the reductions modulo the two rational torsion-splitting primes of the curve group order n. Concretely: n-1 (or rather the structure of the endomorphism-augmented point group) factors, and I hypothesize that the discrete log k relative to base G is congruent, via CRT over small division-polynomial locus evaluations, to a hidden-shift value recoverable from the pair of functions f_l(x) = (numerator of the l-division-polynomial evaluated at [k]G's x-coordinate) for two coprime small primes l, l'. Because gcd(psi_l, psi_l')=1 (already certified), the joint locus is a 0-dimensional variety, and I conjecture the map k |-> (psi_l([k]G).x mod small, psi_l'([k]G).x mod small) is affine-linear in k modulo the respective torsion periods, turning ECDLP into an abelian hidden-subgroup / hidden-shift instance whose group is Z/ord_l x Z/ord_l' rather than the full Z/n.
- **Sub-claim:** Take the toy curve E: y^2 = x^3 + 7 over F_p with p=79 (a prime of the same curve form). Let G=(x_G,y_G) be a fixed point of E(F_p) of order ord = its point order. Define the finite map T(k) = ( x-coordinate of [k]G reduced mod p , via the two division polynomials psi_2 and psi_3 evaluated formally). PRECISE ISOLABLE SUB-CLAIM: (a) as polynomials in x over F_p, gcd(psi_2, psi_3) = 1 (they are coprime, so the joint zero-locus is 0-dimensional and Res_x(psi_2,psi_3) is a nonzero constant of F_p); AND (b) the hypothesized affine-LINEARITY of the coordinate map fails: defining X(k) = x-coordinate of [k]G in F_p for k=1..ord, the FIRST finite difference D(k)=X(k+1)-X(k) mod p is NOT constant, and equivalently the SECOND finite difference X(k+2)-2X(k+1)+X(k) mod p is NOT identically zero. This EXACTLY refutes the linear-HSP form of the reduction: the map k |-> [k]G.x is not affine-linear in k, so ECDLP does not collapse to a linear hidden-shift over Z/ord via these coordinate/division-polynomial evaluations.
- **Evidence:** `p = 79 base G = (1, 18) order = 67
resultant(psi2^2,psi3) mod p = 63 (nonzero => coprime)
num distinct first differences = 49 of 65
nonzero second differences = 62 of 64
CERT_OK`

## `sig:e78c1a83e364` — axis: algebraic (endomorphism ring / CM, isogeny graph, torsion structure E[n]) — outcome: **rejected-by-gate** (gate 0/2)
- **Hypothesis:** secp256k1 has j-invariant 0 and CM by the order Z[omega] (omega a primitive cube root of unity), giving the explicit degree-1 endomorphism beta: (x,y) -> (zeta*x, y) with zeta^3=1 in F_p, whose eigenvalue lambda on any prime-order subgroup satisfies lambda^2+lambda+1=0 mod n. The novel angle: study how the *pair* of Frobenius pi and the CM endomorphism beta jointly act on the l-torsion module E[l] as a Z[omega]-module, i.e. treat E[l] not as a 2-dimensional F_l-vector space but as a rank-1 module over the finite ring (Z[omega]/l). The hypothesis is that for primes l that SPLIT in Z[omega] (l == 1 mod 3), the Frobenius pi decomposes as a product of two CM-conjugate prime-ideal actions, and the GLV eigenvalue lambda is forced to be an explicit ratio of Frobenius components pi_p1/pi_p2 mod l. This ties the GLV lambda (already used only for speedup) to the *splitting type of l in the CM field*, potentially exposing a torsion-indexed invariant that correlates lambda's multiplicative order across many l with the factorization pattern of n +/- known CM-norm forms.
- **Sub-claim:** For secp256k1 (p = 2^256-2^32-977, n the group order, t = p+1-n the Frobenius trace), the following exact facts hold and jointly instantiate the CM/GLV/splitting-type angle: (A) p == 1 mod 3, so a primitive cube root of unity zeta exists in F_p with zeta^3==1, zeta!=1, and the endomorphism beta:(x,y)->(zeta*x,y) preserves the curve because (zeta*x)^3+7 == x^3+7 identically mod p. (B) The GLV eigenvalue lambda (a known root of T^2+T+1 mod n) satisfies lambda^2+lambda+1==0 mod n, equivalently lambda*(lambda+1) == n-1 mod n, and beta acts as scalar lambda: the curve's CM discriminant is -3. (C) For a chosen small prime l with l==1 mod 3 that SPLITS in Z[omega], the Frobenius characteristic polynomial C(T)=T^2 - t*T + p factors mod l into two DISTINCT linear factors T-pi1, T-pi2 with pi1*pi2==p mod l, pi1+pi2==t mod l, and the GLV relation polynomial M(T)=T^2+T+1 also factors mod l with a root lam_l such that lam_l == pi1*inverse(pi2) mod l or lam_l == pi2*inverse(pi1) mod l — tying GLV lambda to the ratio of the two CM-conjugate Frobenius prime-ideal actions. (D) Splitting-type criterion: resultant_T(M(T), C(T)) == 0 mod l (equivalently gcd(M,C) mod l is nontrivial) for such split l, whereas for a prime l' with l'==2 mod 3 (inert in Z[omega]) the resultant is nonzero mod l'.
- **Evidence:** `CERT_OK`

