# Hypothesis ledger (auto-appended; see notes/HYPOTHESIS_EXPLORER.md)

## `sig:9b52d127fa4b` — axis: index-calculus / factor-base (Semaev summation, Weil descent, decomposition bases) — outcome: **rejected-by-gate** (gate 0/1)
- **Hypothesis:** The residue field of the canonical reduction of secp256k1's endomorphism ring Z[β] (where β² + β + 1 = 0 models the Frobenius trace structure mod p) admits a *non-trivial* factorization of the norm form N(a + bβ) = a² + ab + b² into a product of two *independent* index-calculus factor bases when restricted to points whose x-coordinates lie in a specific arithmetic progression modulo a small prime divisor of the discriminant -3. Specifically: if P = (x, y) ∈ E[F_p] and x ≡ r (mod ℓ) for ℓ | gcd(p−1, p+1, p²−p+1) and small ℓ, then the endomorphic action [β]P interleaves the x-locus coordinate in a way that *decouples* smooth-norm factorizations along two coprime congruence classes, breaking the single-base requirement of classical Semaev decomposition.
- **Sub-claim:** For secp256k1 over F_p (p = 2^256 - 2^32 - 977), the norm form N(a+bβ) = a² + ab + b² of endomorphism ring elements Z[β] (β² + β + 1 = 0) restricted to points (x,y) with x in arithmetic progressions x ≡ r (mod ℓ) for ℓ ∈ {3, 5} and r ∈ {0,...,ℓ-1}, exhibits a *decoupling property*: specifically, the resultant Res_x(C(x), P_ℓ(x)) where C(x) = x³ + 7 is the curve equation and P_ℓ(x) = ∏_{i=0}^{k} (x − r − iℓ) for k=⌊100/ℓ⌋ encodes point x-coordinates in the progression. For a representative sample of 20 distinct (r,ℓ) pairs, the norm factorization pattern of a² + ab + b² for (a,b) = (x,1) values in these progressions produces at least TWO disjoint smooth-norm subsets S₁, S₂ (each with ≥2 elements) such that gcd(max smooth factors in S₁, max smooth factors in S₂) < min(max smooth factors in S₁, max smooth factors in S₂) / 2, demonstrating non-trivial factor base independence. We verify: (i) the resultant is non-zero (confirming generic position), (ii) for each (r,ℓ) at least 5 x-values land in [0,p), (iii) the norm N(x+β) factors into coprime smooth components across at least 2 disjoint residue classes, and (iv) this decoupling does NOT occur when ℓ ∤ gcd(p−1, p+1) (negative control).
- **Evidence:** `3):
  Pair1 factors: {19, 7, 43, 13, 31}
  Pair2 factors: {97, 67, 37, 7, 73, 13, 79, 19, 31}
  Shared: {19, 31, 13, 7}
Resultant (r=0, ℓ=3) is non-zero mod p: confirmed.
CERT_OK`

## `sig:5fc6269dcd08` — axis: algebraic (endomorphism ring / CM, isogeny graph, torsion structure E[n]) — outcome: **parked**
- **Hypothesis:** The endomorphism ring End(secp256k1) admits a hidden Z-module structure that permits rational-point lifting between the curve and its Frobenius conjugate via an explicit quaternionic correspondence. Specifically: there exist small-norm elements φ ∈ End(E) and an associated finite set of *auxiliary torsion points* T ⊂ E[N] (for N polynomial in log p but independent of the discrete log instance) such that any point P satisfying a non-generic torsion-pairing relation generates a family of congruence constraints on the Frobenius endomorphism's action—constraints that force the discrete log exponent into a proper subgroup of Z/ord(P) that admits factorization by the algebraic norm form N(φ).
- **Sub-claim:** For secp256k1 over F_p with p = 2^256 - 2^32 - 977, the Frobenius endomorphism π satisfies the minimal polynomial μ_π(x) = x² - tx + p where t is the trace of Frobenius. For the 15-torsion divisibility polynomial ψ₁₅(x), when we compute the resultant Res_x(ψ₁₅(x), y² - x³ - 7) to obtain the 15-torsion points, there exists a Galois-stable secondary component: specifically, we isolate the polynomial Q(x) = Res_x(ψ₁₅(x), (y² - x³ - 7)²) and verify that gcd(Q(x), x² - tx + p) is nontrivial (degree ≥ 1) over Z, indicating a hidden congruence class linking torsion geometry to the Frobenius norm form.
- **Evidence:** `print(f"    |t| check: {abs(t)} ≤ 2√p = {2 * isqrt(p)}: {abs(t) <= 2*isqrt(p)}")
                                                 ^^^^^
NameError: name 'isqrt' is not defined`

