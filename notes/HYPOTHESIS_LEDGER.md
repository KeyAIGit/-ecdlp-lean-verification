# Hypothesis ledger (auto-appended; see notes/HYPOTHESIS_EXPLORER.md)

## `sig:dce293978cdf` — axis: index-calculus / factor-base (Semaev summation, Weil descent, decomposition bases) — outcome: **rejected-by-gate** (gate 0/2)
- **Hypothesis:** For secp256k1 over its prime field F_p, there exists a rational map of degree d of the elliptic curve to a hyperelliptic curve of genus 2 over F_p such that low-height x-coordinates (|x| < B) map to divisors on the Jacobian whose Mumford representation has coefficients bounded by some polynomial in log(p), enabling more efficient relation generation in an index-calculus attack by decomposing steps of the form (P + Q) where P, Q are factor base points.
- **Sub-claim:** For the substitution u = x^2 (i.e. a=0 in the candidate map (u,v)=(x^2+a, y)), the resultant Res_x(y^2 - x^3 - 7, u - x^2) as a polynomial in u and y equals exactly (y^2-7)^2 - u^3, hence the elimination of x produces the curve y^4 - 14 y^2 + 49 - u^3 = 0, which is NOT a genus-2 hyperelliptic curve of the form v^2 = sextic in u (it is quartic in v = y), so this specific candidate map does NOT yield the claimed genus-2 hyperelliptic model. Concretely we verify the exact resultant identity and that the resulting relation is degree 4 (not 2) in y, refuting the (u,v)=(x^2+a,y) route to a genus-2 v^2=f(u) model.
- **Evidence:** `CERT_OK`

## `sig:179e20576d88` — axis: geometric (higher genus, abelian varieties, moduli, covers) — outcome: **rejected-by-gate** (gate 0/2)
- **Hypothesis:** The Jacobian of the genus-2 hyperelliptic curve C: y^2 = x^7 + 7 over F_p (p = secp256k1 prime) has endomorphism ring isomorphic to a maximal order in a quaternion algebra, providing non-commutative endomorphisms that enable a reduction of the ECDLP on secp256k1 to a DLP on J(C) where the embedding degree is smaller than that of secp256k1.
- **Sub-claim:** For the curve C: y^2 = x^7 + 7 over F_p with p = secp256k1 prime, the naive affine point count over the small prime field F_r with r = 101 (a prime of the same residue class, r != p) does NOT match a genus-2 Frobenius structure trivially; concretely we verify an EXACT, isolable arithmetic fact underlying any L-polynomial computation: the number of affine points on C over F_r equals sum over x in F_r of (1 + chi(x^7+7)) where chi is the quadratic character (Legendre symbol), and we compute this exact integer N_affine, then verify the projective smooth model has genus 2 by confirming the defining polynomial f(x)=x^7+7 is separable over F_r (gcd(f,f')=1), so C is a genuine genus-2 hyperelliptic curve. We assert the exact value N_affine and separability, giving the first exact ingredient (a Frobenius trace input a_1 = r + 1 - #C(F_r) restricted to the affine part) needed to build L(T).
- **Evidence:** `r = 101
genus = 3
separable = True
N_affine = 101
CERT_OK`

## `sig:85e9cbbc6745` — axis: cross-domain analogy (lattices, codes, dynamical systems, tensor networks, category theory) — outcome: **refuted**
- **Hypothesis:** The addition law on the Kummer variety of secp256k1, when interpreted via the Lattice Isomorphism Problem, yields a hidden affine singularity: the x-coordinate of nP ± mQ satisfies a conic intersection whose resultant over F_p factors into linear forms over F_{p^2} if and only if the discrete log of Q base P is a small integer combination of known multiples.
- **Sub-claim:** The overarching hypothesis is FALSE at its first isolable step. Concretely: the quantity D_{1,1}(k) := x_{P+Q} + x_{P-Q} - 2*x_P, where Q=kP and all x-coordinates come from the group addition law on secp256k1, is NOT a polynomial in k of any fixed low degree over F_p; and the alleged 'resultant' construction is vacuous because x_{P+Q}, x_{P-Q} are algebraic (not polynomial) functions of k. We test the exact, well-defined algebraic core that the angle would require: for P+Q and P-Q with Q=kP on the actual curve, the sum s(k)=x_{P+Q}+x_{P-Q} and the value 2*x_P do NOT coincide, i.e. D_{1,1}(k) != 0, for the small integer values k that a '4-dimensional lattice of linear factors' would need to be roots. If the hypothesis held, k would be recoverable as a small integer root of a degree-4 form; we show that for a concrete generator, the addition-law identity linking x_{P+Q}, x_{P-Q}, x_P is the standard symmetric-function relation (x_{P+Q}+x_{P-Q}) = a rational function of x_P and x_Q with denominator (x_P - x_Q)^2, and that this relation is NOT linear/affine in k, refuting the claimed conic-linear-factorization mechanism. We verify the exact identity x_{P+Q}*x_{P-Q}*(x_P - x_Q)^2 = (x_P*x_Q + 7)^2 (the true sum/product relation for secp256k1 with b=7) as the correct algebraic law, and confirm the hypothesized D_{1,1}=0 fails for the tested small k.
- **Evidence:** `fp09itng/cert.py", line 61, in <module>
    assert lhs_prod == rhs_prod, ('product law fails at k=%d' % k)
           ^^^^^^^^^^^^^^^^^^^^
AssertionError: product law fails at k=2`

## `sig:576950abfe33` — axis: analytic / p-adic (formal groups, canonical heights, Frobenius / L-functions) — outcome: **refuted**
- **Hypothesis:** For an elliptic curve E over ℤ_p such that #E(F_p) has a prime factor ℓ ≠ p, the p-adic log of a point P of order ℓ, denoted log_E(P), lies in the kernel of a certain p-adic regulator map R: K_2(E) → p-adic cohomology, forcing a nontrivial algebraic relation between the canonical height ĥ(P) and the p-adic logarithm of the x-coordinate. This relation does not require that ℓ be bounded or that the curve be ordinary, but follows from the structure of the formal group and the ℓ-adic GKZ hypergeometric system.
- **Sub-claim:** For secp256k1 (y^2 = x^3 + 7 over F_p, p = 2^256 - 2^32 - 977), the formal group logarithm log_E(t) = t + sum a_k t^k satisfies the standard normalization (leading coefficient 1, and each coefficient a_k has denominator dividing k so p does not divide the denominators for the first N terms since p is huge), AND the resultant R_3 = Res_t(psi_3(t), Lred(t)) computed over F_p is a nonzero element of F_p — i.e. R_3 not divisible by p — where psi_3 is the reduction of the 3-division polynomial expressed in the formal parameter t = -x/y (via the substitution giving x = 1/t^2 + ... ), and Lred(t) is the truncated formal logarithm. Concretely we check: (a) the 3-division polynomial phi_3(x) = 3x^4 + 6*7*x^2 ... for y^2=x^3+7 equals 3x^4 + 42x^2 + ... has the exact known form 3x^4 + 6*a*x^2 + 12*b*x - a^2 with a=0,b=7, i.e. phi_3 = 3x^4 + 84x - 0 = 3x^4 + 84x; (b) the formal logarithm coefficients through order 7 for the invariant differential of E match the recursion from the formal group law; (c) Res_x(phi_3(x), f_log_num(x)) mod p is nonzero, where f_log_num is a fixed low-degree polynomial coprime to phi_3, certifying the coprimality (R_3 not ≡ 0 mod p) that the angle requires as its first isolable test.
- **Evidence:** `last):
  File "/tmp/tmp9aazit24/cert.py", line 68, in <module>
    assert c1 == 1, 'log leading coeff not 1: %s' % c1
           ^^^^^^^
AssertionError: log leading coeff not 1: 3`

## `sig:bf6acaba0ef0` — axis: algebraic (endomorphism ring / CM, isogeny graph, torsion structure E[n]) — outcome: **refuted**
- **Hypothesis:** The endomorphism ring of secp256k1 contains the quadratic order Z[ω] where ω is a primitive 3rd root of unity, and the Frobenius endomorphism π satisfies π² - tπ + p = 0. Consider the endomorphism ψ = ω + π. The discriminant of the order Z[ψ] is expected to be a divisor of disc(Z[π]) * disc(Z[ω]) / d² for some integer d, and we conjecture that disc(Z[ψ]) is much smaller than p, specifically O(√p), enabling a reduction of the ECDLP to a class group action in a CM order of small discriminant.
- **Sub-claim:** Let ψ = ω + x where ω²+ω+1=0 and x²-tx+p=0 with t the secp256k1 Frobenius trace (t = p+1-n, computed from the known p and n). Define the degree-4 characteristic polynomial g(y) of ψ over Q as g(y) = Res_x( x²-tx+p , (y-x)²+(y-x)+1 ), obtained by eliminating x via the substitution ω = y-x subject to ω²+ω+1=0. CLAIM (a precise, exactly-checkable test of the angle): (a) g(y) is a monic (up to sign normalization) degree-4 integer polynomial; (b) the discriminant D = disc(g) is an EXACT integer; (c) contrary to the hypothesis that |disc(Z[ψ])| = O(√p), the actual |D| is NOT less than p (in fact it vastly exceeds p and exceeds √p by many orders of magnitude), so the conjectured smallness fails; and (d) the number-field discriminant test disc(g) divides (t²-4p)*(-3) up to square factors FAILS because ψ generates the compositum Q(π,ω) = Q(√(t²-4p), √-3), a degree-4 field whose discriminant is governed by BOTH quadratic discriminants, so disc(g) is of size ~(t²-4p)²·(-3)-magnitude ~ p², definitively refuting the O(√p) claim.
- **Evidence:** `recent call last):
  File "/tmp/tmpxcm4hc1g/cert.py", line 10, in <module>
    assert t*t <= 4*p, 'Hasse bound violated'
           ^^^^^^^^^^
AssertionError: Hasse bound violated`

## `sig:790991ed7d2b` — axis: analytic / p-adic (formal groups, canonical heights, Frobenius / L-functions) — outcome: **refuted**
- **Hypothesis:** The p-adic sigma function attached to the formal group of secp256k1 has a zero at a p-adic integer corresponding to the x-coordinate of a rational point of infinite order, implying that p-adic elliptic logarithms of such points satisfy a linear relation over Q_p.
- **Sub-claim:** For the formal group of secp256k1 (y^2 = x^3 + 7 over F_p), the p-adic sigma function sigma(t) as a power series in the formal parameter t = -x/y begins sigma(t) = t + O(t^3), with the coefficient c1 of t^3 given by c1 = -a1/12*... but concretely: the formal group law and its associated formal logarithm log(t) = sum_{k>=1} b_k t^k satisfy b_1 = 1 and the recursion b_k = c_{k-1}/k where the invariant differential omega = (1 + sum a_i t^i) dt is derived from the Weierstrass expansion. The isolable, exact check is: the formal logarithm of the secp256k1 formal group, computed by integrating omega(t) = sum_{m>=0} coefficient t^m dt term-by-term from the Weierstrass parametrization x(t)=t/w, y(t)=-1/w with w satisfying w = t^3 + a6*w^3 (a1=a2=a3=a4=0, a6=7), yields a normalized logarithm L(t) = t + O(t^4) whose leading nontrivial correction first appears at t^7 with coefficient exactly -7/7 = -1 (i.e. the coefficient of t^7 in L(t) is -1), and consequently sigma(t) = t * exp(-integral of (something)) is odd with NO zero other than t=0 among its computed low-order truncation — verified by checking sigma(t) mod t^12 is an odd series with unit leading coefficient, so it does NOT vanish at any nonzero small t, refuting the hypothesized extra zero at leading order.
- **Evidence:** `line 45, in <module>
    assert c7 == Rational(-a6,7), f'coeff t^7 = {c7}, expected {Rational(-a6,7)}'
           ^^^^^^^^^^^^^^^^^^^^^
AssertionError: coeff t^7 = 3, expected -1`

## `sig:362f33602713` — axis: geometric (higher genus, abelian varieties, moduli, covers) — outcome: **rejected-by-gate** (gate 0/2)
- **Hypothesis:** The secp256k1 curve admits a degree-4 cover to a genus-2 hyperelliptic curve defined by a degree-4 equation whose coefficients come from the 2-torsion polynomial ψ_2(x)=x^3+7, such that the Jacobian of the genus-2 curve is (2,2)-isogenous to the product of secp256k1 with another elliptic curve E' whose embedding degree is less than 100.
- **Sub-claim:** For secp256k1 (y^2 = x^3 + 7 over F_p), the 2-division polynomial is psi_2(x) = x^3 + 7. Consider the family of genus-2 hyperelliptic curves y^2 = Q(x) with Q(x) = x^4 + a*x^3 + b*x^2 + c*x + d. The FIRST isolable test of the cover hypothesis is the resultant/branch-locus consistency: (i) Res_x(psi_2(x), Q(x)) is the explicit symmetric expression obtained by evaluating Q at the three roots of psi_2, and equals the value produced by sympy's resultant; (ii) for the specific quartic Q(x) = psi_2(x)*(x - r) + s (the natural degree-4 lift branched along psi_2), the resultant Res_x(psi_2, Q) equals s^3 exactly (i.e. it is INDEPENDENT of a,b,c,d beyond r,s and equals s^3), so the cover is branched at the 2-torsion points precisely when s ≡ 0 mod p — and moreover psi_2(x) = x^3+7 is SEPARABLE mod p (gcd(psi_2, psi_2') = 1), a necessary condition for the (2,2)-split Jacobian construction. All identities are verified as exact polynomial identities over GF(p).
- **Evidence:** `CERT_OK`

## `sig:f089ee732433` — axis: cross-domain analogy (lattices, codes, dynamical systems, tensor networks, category theory) — outcome: **parked**
- **Hypothesis:** The secp256k1 curve, via its endomorphism ring and the Lyness 5-cycle dynamical system on its Jacobian, gives rise to a 3-dimensional tensor T_{ijk} = Tr( x_i * y_j * z_k ) over triples of points in the cycle. The discrete logarithm problem for a point P is equivalent to the condition that T has rank ≤ 2, whereas generic points yield rank 3.
- **Sub-claim:** 
- **Evidence:** `(rigour tier produced no checkable sub-claim/script)`

## `sig:0fb4fffdee8d` — axis: reduction / equivalence (to factoring, to other DLPs, to hidden-subgroup variants) — outcome: **rejected-by-gate** (gate 0/2)
- **Hypothesis:** The discrete log problem in secp256k1 is equivalent to factoring a specific discriminant Δ derived from the endomorphism ring of the quadratic twist E' over F_p^2, where E' has j-invariant 1728.
- **Sub-claim:** The quadratic twist of secp256k1 by any fixed non-square d in F_p has the SAME j-invariant 0 (NOT 1728) as the original curve, because quadratic twisting preserves the j-invariant. Concretely: for E: y^2 = x^3 + 7 (a=0, b=7), j = 1728*4a^3/(4a^3+27b^2) = 0 mod p, and its quadratic twist E^d: y^2 = x^3 + 7*d^3 (obtained by the standard twist substitution x->x/d, y->y/d^(3/2) giving a'=a*d^2=0, b'=b*d^3) also has j = 0 mod p. This DISPROVES the hypothesis's central premise that j(E')=1728: since j is a twist invariant and secp256k1 has j=0, no quadratic twist can have j=1728 unless 0=1728 mod p (which is false). We verify: (i) j(E)=0, (ii) d is a non-square mod p, (iii) the twisted coefficients are a'=0, b'=7*d^3, (iv) j(E')=0, and (v) 1728 != 0 mod p.
- **Evidence:** `CERT_OK`

## `sig:a2ecf0cc9471` — axis: cross-domain analogy (lattices, codes, dynamical systems, tensor networks, category theory) — outcome: **rejected-by-gate** (gate 0/2)
- **Hypothesis:** The secp256k1 group law (x_n, y_n) = nP yields a recurrence that, after normalizing the curve to the universal elliptic curve over the base of the discrete Painlevé VI equation, satisfies a τ-function identity with the Weierstrass σ-function for g_2=0, g_3 = -16*7, such that the discrete logarithm n corresponds to the shift of a divisor on the spectral curve of the Lax representation of the Painlevé system.
- **Sub-claim:** For three consecutive multiples of a point on y^2 = x^3 + 7 (Weierstrass form, additive recurrence), the x-coordinates satisfy the exact 3-term chord-tangent relation, which is the correct starting point for any Painleve VI / QRT embedding. Concretely: let P = 2G, 2P, 3P have x-coordinates x1, x2, x3 (where G is a symbolic point on the curve). The secp256k1 group law forces x1, x2, x3 to obey the symmetric QRT-type biquadratic relation coming from collinearity of P, Q, -(P+Q). We verify the ISOLABLE sub-fact: for a symbolic point (a,b) with b^2 = a^3 + 7, the x-coordinate of 3(a,b) minus a satisfies an EXACT rational identity in a alone (after reducing b^2), and that the doubling+addition group law is internally consistent, i.e. x(3P) computed as x(2P + P) equals x(P + 2P), and the numerator of [x(3P) - x_doubling_addition_formula] reduces to 0 modulo (b^2 - a^3 - 7). This is the genuine algebraic recurrence that the Painleve embedding must reproduce; a nonzero resultant here would immediately falsify the angle.
- **Evidence:** `CERT_OK`

## `sig:f4a62d5402ad` — axis: index-calculus / factor-base (Semaev summation, Weil descent, decomposition bases) — outcome: **rejected-by-gate** (gate 0/2)
- **Hypothesis:** For secp256k1, let P be the generator. The map φ: Z² → F_p given by (a,b) → x(aP+bP) may satisfy a low-degree rational function relation when restricted to small integers a,b, due to the group law and the structure of the curve's function field over F_p. This could create a factor base of such x-coordinates linked by algebraic relations of degree < 20.
- **Sub-claim:** For secp256k1's curve y^2 = x^3 + 7 over F_p, let x2(x,y) and x3(x,y) be the affine x-coordinate rational functions of 2P and 3P given by the standard group law (in terms of x=x(P), y=y(P)). Eliminating y via y^2 = x^3 + 7, the minimal polynomial of X=x(2P) over F_p(x) is M2(X,x) := (X)*(2y_slope_denom) form reducible to a degree-1 polynomial in X of the form (4(x^3+7))*X - (x^4 - 56x) (i.e. the duplication formula x(2P) = (x^4 - 2*7*4x)/(4(x^3+7)) = (x^4-56x)/(4(x^3+7))), and similarly x(3P) is a rational function of degree 9 in x with NO shared y-dependence. CONCRETE CHECK: (a) the x(2P) formula (x^4 - 56x)/(4(x^3+7)) equals the group-law-derived x(2P) as an identity in F_p[x] (after clearing denominators, modulo y^2=x^3+7); (b) the numerator N2(X,x)=4(x^3+7)X-(x^4-56x) and numerator N3(X,x) of x(3P)-X, both treated as polynomials in X over F_p(x), have Res_X(N2,N3) that is NON-ZERO as a polynomial in x (i.e. no nontrivial common factor of degree<20 linking x(2P) and x(3P) generically), demonstrating the hypothesized low-degree factor-base algebraic relation does NOT exist — the resultant is a nonzero polynomial in x meaning x(2P) and x(3P) are algebraically independent as functions of x only through x itself.
- **Evidence:** `deg_x resultant = 12
CERT_OK`

## `sig:d754ed316094` — axis: algebraic (endomorphism ring / CM, isogeny graph, torsion structure E[n]) — outcome: **rejected-by-gate** (gate 0/2)
- **Hypothesis:** The action of the Frobenius endomorphism F on the 2-torsion of secp256k1, combined with the CM endomorphism ω (a primitive 3rd root of unity), defines a 2-dimensional F2-vector space that becomes a module over the non-commutative ring F2[F, ω]/(F^2 - F + 1, ω^2 + ω + 1, Fω - ωF). This module has dimension >2 over F2, implying that E[2] carries a hidden non-Galois structure that contradicts the group law when lifted to the CM field Q(√-3).
- **Sub-claim:** For secp256k1 (y^2 = x^3 + 7 over F_p), the 2-torsion E[2] is the F_2-vector space spanned by the affine 2-torsion points (r_i, 0) where r_i are the roots of the 2-division polynomial x^3 + 7. CONCRETE ISOLABLE TEST: (a) The Frobenius endomorphism F acts on E[2] as the identity, because x^3 + 7 is IRREDUCIBLE over F_2 (so p is a cube mod... no) — precisely, x^3+7 = x^3+1 = (x+1)(x^2+x+1) mod 2, hence over F_2 the polynomial factors, and F acts on E[2] with characteristic polynomial equal to (x - 1)(x^2 - x + 1) reduced... We test the EXACT claim that Frobenius restricted to E[2] satisfies its reduction: since #E(F_2) for the curve y^2=x^3+7 mod 2 determines the 2-torsion Galois action, we verify F^2 - t*F + 2 = 0 on E[2] with the correct trace t, and that the hypothesis' asserted relation Res(omega^2+omega+1, F^2 - F + p) mod 2 is a specific value. (b) The endomorphism omega (multiplication by a primitive cube root of unity, giving GLV) exists with omega^2 + omega + 1 = 0, and omega acts on x-coordinates as x -> beta*x where beta^3 = 1 mod p. We verify beta^3 = 1 mod p, beta != 1, and that omega maps 2-torsion x-roots of x^3+7 to other 2-torsion x-roots (i.e. beta*r is again a root of x^3+7 mod p), confirming omega permutes E[2]. (c) The hypothesis claims (F o omega)^2 + (F o omega) + I = 0 fails on E[2]; we DISPROVE the exotic 'dimension >2' conclusion by showing E[2] is genuinely 2-dimensional over F_2 (x^3+7 mod 2 is separable / splits appropriately) and that Res(omega^2+omega+1, F^2 - F + p) mod 2 equals its exactly-computed value, so no contradiction arises. The precise checkable facts: (i) beta^3 == 1 mod p, beta != 1; (ii) for each root r of x^3+7 over F_p (in extension), beta*r is also a root; (iii) Res(y^2+y+1, y^2-y+p) mod 2 == exactly the computed integer; (iv) x^3+7 is separable mod 2 so E[2] has exactly F_2-dimension 2 (order 4 group), refuting 'dimension >2'.
- **Evidence:** `CERT_OK`

## `sig:c92c5e9d66c9` — axis: index-calculus / factor-base (Semaev summation, Weil descent, decomposition bases) — outcome: **refuted**
- **Hypothesis:** The degree of the m-th Semaev polynomial S_m(x_1,...,x_m) over F_p for secp256k1, when restricted to a subfield of small prime l via reduction modulo l of the coefficient field, is always divisible by a fixed integer r>1 for m≥3, regardless of the choice of distinct x-coordinates for the base points, due to a mod-l symmetry arising from the action of the Frobenius endomorphism on the set of solutions to the summation polynomial.
- **Sub-claim:** For secp256k1's curve constants (a=0, b=7) the third Semaev polynomial is S_3(x1,x2,x3) = (x1-x2)^2 x3^2 - 2((x1+x2)(x1 x2 + a) + 2b) x3 + ((x1 x2 - a)^2 - 4b(x1+x2)) with a=0,b=7. PRECISE isolable test: reduce the coefficients of S_3 modulo l=3 and l=5, then for several choices of distinct (a1,a2) in F_l substitute x1=a1,x2=a2 and form the univariate polynomial R(a1,a2,X) over GF(l). The concrete checkable fact (contradicting the divisibility-by-r>1 hypothesis) is: the GCD of the X-degrees of R(a1,a2,X) taken over all distinct-in-F_l choices of (a1,a2) equals 2 for l=3 but equals 1 for l=5 (because some choice gives a degree-1 polynomial mod 5 when the leading (x1-x2)^2 coefficient vanishes mod 5 while a linear term survives). Hence no fixed r>1 divides all degrees for m=3, refuting the stated 'always divisible by fixed r>1' hypothesis. We verify: (i) S_3 matches the resultant construction over F_p symbolically, and (ii) the degree-GCD is 2 for l=3 and 1 for l=5.
- **Evidence:** `most recent call last):
  File "/tmp/tmpn46qan64/cert.py", line 64, in <module>
    assert g5 == 1, ('l=5 degree-GCD', g5)
           ^^^^^^^
AssertionError: ('l=5 degree-GCD', 2)`

## `sig:1d7494d9396e` — axis: algebraic (endomorphism ring / CM, isogeny graph, torsion structure E[n]) — outcome: **rejected-by-gate** (gate 0/2)
- **Hypothesis:** The endomorphism ring of secp256k1 is isomorphic to the maximal order Z[(1+√-3)/2] of Q(√-3), but the curve has CM by the non-maximal order Z[√-3] because p ≡ 1 mod 3 but not mod 9. This mismatch forces a low-degree 3-isogeny from E to a curve with CM by the maximal order, revealing a hidden endomorphism of degree 3 that acts as a trapdoor on a 1/3 fraction of points.
- **Sub-claim:** The j-invariant of secp256k1 is 0. Consequently the modular polynomial Phi_3(0, x) over F_p (p = 2^256-2^32-977) is a specific cubic in x (namely x^3 - 15552*x^2 + (8748000)*x - 157464000000000 plus the actual Phi_3(0,x) form), and its roots give the j-invariants of the 3-isogenous curves. We check the EXACT, isolable fact: (a) the curve constant a=0, b=7 gives j=0 mod p; (b) Phi_3(0, x) factors over F_p and has the root x=0 (self/CM 3-isogeny consistent with j=0 curve), i.e. Phi_3(0,0) = 0 mod p; (c) since p = 1 mod 3, the polynomial x^2 + x + 1 splits over F_p, exhibiting a nontrivial cube root of unity (the source of the degree-3 CM endomorphism), and Phi_3(0,x) does NOT have a repeated structure forcing all roots equal — i.e. Phi_3(0,x) has 0 as a root but is not identically a cube of (x - r).
- **Evidence:** `CERT_OK`

