# Finite Weil ambiguity boundary C056

Date: 2026-08-13

This is an abstract finite-representation checkpoint on frozen prime orders only.

For the order-three finite Weil kernel `U` and a Weyl displacement `W(q,p)`, direct completion of the quadratic exponent gives

```text
Tr(U W(q,p))/Tr(U)=zeta^(-(p^2+p*q+q^2)/6).
```

The normalized trace is therefore one quadratic chirp. For `p=0` it is even under `q -> -q`, assumes `(n+1)/2` distinct values, and has exactly flat additive Fourier magnitude `1/sqrt(n)`.

Consequences:

- the canonical metaplectic scalar is not a binary branch selector;
- an even phase cannot equal an anti-invariant target on an odd cycle;
- the compact trace formula presupposes a represented Weyl displacement and does not construct its coordinate from an unrelated encoding;
- a useful odd observable must insert additional oriented data rather than reuse the standard scalar cocycle.

Exact scalar-model replay was completed for frozen prime orders up to 4021. No curve point or production target is part of this checkpoint.

Successor: study the first odd moment and identify precisely which additional coordinate operator it requires.
